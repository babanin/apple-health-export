[pytest]
markers =
    integration: marks tests as integration tests (require Docker/live services)
    e2e: marks tests as end-to-end tests (require full stack)

testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

addopts = -v --tb=short